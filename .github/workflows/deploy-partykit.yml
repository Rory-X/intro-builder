name: Deploy PartyKit

on:
  push:
    branches: [main]
    paths:
      - "apps/partykit/**"
      - "packages/shared/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/workflows/deploy-partykit.yml"
  workflow_dispatch:

concurrency:
  group: deploy-partykit-production
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    name: Verify and deploy PartyKit
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck PartyKit
        run: pnpm --filter @intro-builder/partykit typecheck

      - name: Check PartyKit credentials
        env:
          PARTYKIT_LOGIN: ${{ secrets.PARTYKIT_LOGIN }}
          PARTYKIT_TOKEN: ${{ secrets.PARTYKIT_TOKEN }}
        run: |
          set -euo pipefail
          : "${PARTYKIT_LOGIN:?Missing PARTYKIT_LOGIN secret}"
          : "${PARTYKIT_TOKEN:?Missing PARTYKIT_TOKEN secret}"

      - name: Deploy PartyKit
        env:
          PARTYKIT_LOGIN: ${{ secrets.PARTYKIT_LOGIN }}
          PARTYKIT_TOKEN: ${{ secrets.PARTYKIT_TOKEN }}
        run: pnpm --filter @intro-builder/partykit run deploy

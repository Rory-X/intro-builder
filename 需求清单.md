### 小需求
1. 分享的icon的打开区块需要美观，现在太长方形块了。然后这些打开区块都一个字体，按照导师邀请为标准，导出简历也需要跟着调
2. 新简历与保存按钮挨太近了，需要调整
3。左侧返回的字体字号也是，字体颜色可以变但字号和字体统一
4. 邀请协作模式产品功能有问题，无法取消
5. 左侧编辑区已经大更新，再这个基础上修改：
5.1 整体比例增加像浏览器一样增加5%的效果
5.2 小section的间距优化的不好，小section的定义是教育背景里面的新增的小项目。每个表单标题和上一个表单间距太小了。需要调节的有一定空间
5.3 富文本区的toolbar能够随着拖动条变化而不是写死位置。各icon间距再拉大些
5.4 名字这一栏要独立居大占据头像宽度比例2/3. 光标悬浮时有表单的边框，不悬浮不显示
5.5 头像尺寸在头像位置下方来调节。名字下方用灰色小字说明"点击上传头像（可选，4MB 以内）
5.6 小section能够整行拖动，所有的拖动都补充动画，像模块管理那样的动画
5.7 展开也要增加动画效果
5.8 自定义模块消失了
5.9 导师协作邀请要变蓝色说明已经开启了
5.10 删除的垃圾桶icon不太好
5.11 设置初始模板
5.12 整体层次仍然不够好
5.13 点击笔来修改简历名字，需要有动画效果
5.14 导出简历的字体字号修改


5.13 拖动调整顺序

### 仍然有bug或优化
第一次
所有单行的（个人信息、技能没有动画效果
名字占据宽度2/3，是要放大到对应大小。
头像尺寸改名-尺寸，缩短长度，长度与头像宽度一致，且增加和减少改为上下的三角小icon，点击数字可选择调节区间
拖动调整顺序时，不要实时调整，等调整完成后再渲染--模块管理同样，并且动画很不流程仿照模块管理
深色模式下富文本区要变灰
可以伸缩的大区间右下角要有3条斜线，现在一条不好

第二次
1. 导师协作取消了仍然显示蓝色
2. 展开有动画压缩又没有动画了又偷懒！

---

### 大需求
1. 导师模式的断联设置、还有批注模式的ui和ue设置
2. 文档需要有帮助中心
3. 城市不出来，需要排查各个字段是否有错
4. 协作模式的rollback等机制
5. 作品集等，仍然是自定义的吗？
6. Agent.md 需要更新，否则总是说有本地和内置模板

## 修改记录

| # | 需求 | 改动文件 | 具体修改 | 状态 |
|---|------|----------|----------|------|
| 1 | 分享区块比例更方正 | `editor-client.tsx` | popover `w-80`→`w-56`，`rounded-lg`，输入框 `h-8`，字体统一 | ✅ |
| 1 | 导出简历加粗 | `export-button.tsx` | `font-medium`→`font-semibold` | ✅ |
| 1 | 导出简历字号统一 | `export-button.tsx` | `text-[0.8rem]`→`text-xs` | ✅ |
| 2 | 简历名与保存图标间距 | `editor-client.tsx` | 右组容器 `gap-1`→`gap-2.5` | ✅ |
| 3 | 返回按钮字号统一 | `editor-client.tsx` | `text-sm`→`text-[0.8rem]`（与 Button sm 一致） | ✅ |
| 3 | 简历名字号统一 | `editor-client.tsx` | `text-sm`→`text-[0.8rem]` | ✅ |
| 4 | 邀请协作无法取消 | `invite-collab-dialog.tsx` | 受控 open + 取消协作回到初始状态 | ✅ |
| 5.1 | zoom +5% | `editor-client.tsx` | `[zoom:1.13]`→`[zoom:1.18]` | ✅ |
| 5.2 | 条目间距 | 四个编辑器 + editor-client | `space-y-2`→`space-y-4` | ✅ |
| 5.3 | 富文本 toolbar 自适应 | `rich-text-editor.tsx` | `flex-wrap` + `gap-px`→`gap-1` | ✅ |
| 5.4 | 姓名独立占 2/3 | `basics-editor.tsx` | `flex-[2]` + hover 显示边框 | ✅ |
| 5.5 | 头像尺寸+说明 | `basics-editor.tsx` | PhotoScaleControl 移到头像下方 + 灰色说明 | ✅ |
| 5.6 | 拖动动画 | `item-wrapper.tsx` | 整行 grab + `duration-300` + shadow + translate | ✅ |
| 5.7 | 展开动画 | 四个编辑器 + item-wrapper | `grid-rows-[1fr]/[0fr]` + opacity 过渡 | ✅ |

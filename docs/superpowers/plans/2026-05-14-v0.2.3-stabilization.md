# v0.2.3 Stabilization Notes

## Goal

Prepare the current v0.2.x codebase for a reliable deploy before starting v0.3 AI work.

## v0.2.2 Audit

Implemented in code:

- Dark theme wiring via `next-themes` in `app/layout.tsx`.
- Vercel Blob photo upload in `components/editor/photo-upload.tsx` and `app/api/upload-photo/route.ts`.
- Pragmatic drag-and-drop wrappers in `components/editor/section-wrapper.tsx` and `components/editor/item-wrapper.tsx`.
- Dashboard A4 preview cards and `duplicateResume` in `app/(app)/dashboard/page.tsx` and `app/(app)/dashboard/actions.ts`.
- Puppeteer PDF export in `app/api/pdf/[id]/route.tsx`.

Drift corrected in this iteration:

- `README.md` no longer describes the removed `@react-pdf/renderer` pipeline or per-template `Pdf.tsx` files.
- `.env.example` documents Blob upload and local Postgres without duplicating `BLOB_READ_WRITE_TOKEN`.
- Runtime database setup keeps Neon HTTP for `*.neon.tech` and uses `postgres.js` TCP for local/Docker Postgres.

## Release Checklist

- `pnpm test` — 13 test files / 35 tests passed.
- `pnpm tsc --noEmit` — passed.
- `pnpm lint` — passed with no warnings.
- `pnpm build` — passed.
- Manual smoke — `pnpm dev` was already running at `http://localhost:3000`; verified marketing page renders, login page renders, and `/dashboard` redirects to `/login?next=%2Fdashboard` when unauthenticated.

Remaining manual checks that need real credentials/env:

- Magic Link email delivery requires `AUTH_RESEND_KEY` and `AUTH_EMAIL_FROM`.
- Authenticated dashboard/editor/PDF/share flow requires a migrated database and logged-in session.
- Successful photo upload requires `BLOB_READ_WRITE_TOKEN`; the automated regression covers the unauthenticated and missing-token paths.
